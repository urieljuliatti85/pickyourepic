<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "Pick Up Your Epic!" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Pick Up Your Epic!">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full bg-neutral-950 text-neutral-100 antialiased">
    <main class="min-h-full">
      <%# No layout para que toda pagina mostre suas mensagens, nao so as
          duas que renderizavam o partial por conta propria. %>
      <div class="max-w-4xl mx-auto px-4 pt-4">
        <%= render "shared/flash" %>
      </div>
      <%= yield %>
    </main>
  </body>
</html>
