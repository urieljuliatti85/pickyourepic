<%# An Epic's Pick count, in a frame of its own so PicksController's
    turbo_stream updates it alongside the button. %>
<%= turbo_frame_tag dom_id(epic, :pick_count), target: "_top" do %>
  <%= pluralize(epic.picks.size, "Pick") %>
<% end %>
