<div class="max-w-2xl mx-auto p-4">
  <h1 class="text-2xl font-bold mb-4">Edit Collection</h1>

  <%= render "form", collection: @collection %>

  <div class="mt-4">
    <%= link_to "Back", collection_path(@collection), class: "px-4 py-2 border border-gray-300 rounded hover:bg-gray-100" %>
  </div>
</div>
